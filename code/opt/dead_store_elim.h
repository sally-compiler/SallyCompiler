
Set<Ast_Declaration *, Memory_Write *>

    void dead_store_elim(Program_IR *prog) {
    for (auto f : prog->procedures) {
        for (auto bb : f->blocks) {
            for (auto v : bb->insts)
        }
    }
}
