
int A[10][10];

int get_row(int r[]) {
    return getarray(r);
}

int main() {
    int i = 0, j, k;
    while(i < 10) {
        getarray(A[i]);
        i = i + 1;
    }

    j = 0;
    while(j < 10) {
        k = 0;
        while(k < 10) {
            putint(A[j][k]); putch(10);
            k = k + 1;
        }
        j = j + 1;
    }

    return 0;
}
